import AppLayout from "../../layout/Layout";
import LoginForm from "../../login/LoginForm";

const LoginPage = () => {
  return (
    <AppLayout>
      <LoginForm />
    </AppLayout>
  );
};

export default LoginPage;
